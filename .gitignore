#R001: Ignore local SwiftPM build output.
.build/
#R005: Ignore local Xcode workspace metadata.
DerivedData/
*.xcuserdatad/
*.xcuserstate
#R010: Keep sources, tests, and Package.swift tracked by not adding broad globs.
#R015: Remove previously tracked artifacts with `git rm -r --cached` rather than deleting local files.
#R020: Enforce that ignored artifact paths remain absent from `git ls-files`.
#R025: Guard `.build/` ignore behavior with an automated shell test.
